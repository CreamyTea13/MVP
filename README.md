# Everyday Engine

Source repository for the Everyday Engine static website.

## Deploy
Vercel:
- Build command: `npm run build`
- Output directory: `dist`

The build creates the full 20-tool site, privacy/terms/contact pages, canonical URLs, robots.txt, and a sitemap when Vercel provides the production URL.

No database or paid API is required for v2.
